import 'package:flutter/material.dart';

class AssistantrUi extends StatelessWidget{
    @override
  Widget build(BuildContext context) {
    return Container(
     
      child: Center(
        child: Text('assistant UI '),
      ),
    );
  }
}
import 'package:flutter/widgets.dart';

class DoctorUi extends StatelessWidget{
    @override
  Widget build(BuildContext context) {
    return Container(
     
      child: Center(
        child: Text('Doctor UI '),
      ),
    );
  }
}